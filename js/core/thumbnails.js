import{buildMaze,drawEndpointMarkers}from"../modules/maze-engine.js";

function drawMaze(canvas,page){const ctx=canvas.getContext("2d"),settings=page.settings||{},cols=Math.max(6,Number(settings.cols)||18),rows=Math.max(8,Number(settings.rows)||24),maze=buildMaze(cols,rows,Number(page.seed)||1,settings.endpoints),w=canvas.width,h=canvas.height,margin=14,top=31,areaW=w-margin*2,areaH=h-top-margin,cell=Math.min(areaW/cols,areaH/rows),mazeW=cell*cols,mazeH=cell*rows,startX=(w-mazeW)/2,startY=top+(areaH-mazeH)/2;ctx.clearRect(0,0,w,h);ctx.fillStyle="#fff";ctx.fillRect(0,0,w,h);ctx.fillStyle="#111827";ctx.textAlign="center";ctx.font="700 11px Arial";ctx.fillText((page.title||"Maze").slice(0,30),w/2,18);ctx.strokeStyle="#111827";ctx.lineWidth=Math.max(1,Math.min(2.2,(Number(settings.lineWidth)||5)/3));ctx.beginPath();for(let y=0;y<rows;y++)for(let x=0;x<cols;x++){const px=startX+x*cell,py=startY+y*cell,walls=maze.cells[y][x].w;if(walls[0]){ctx.moveTo(px,py);ctx.lineTo(px+cell,py)}if(walls[1]){ctx.moveTo(px+cell,py);ctx.lineTo(px+cell,py+cell)}if(walls[2]){ctx.moveTo(px,py+cell);ctx.lineTo(px+cell,py+cell)}if(walls[3]){ctx.moveTo(px,py);ctx.lineTo(px,py+cell)}}ctx.stroke();drawEndpointMarkers(ctx,maze,startX,startY,cell,true)}

function drawColoring(canvas,page){const ctx=canvas.getContext("2d"),w=canvas.width,h=canvas.height;ctx.fillStyle="#fff";ctx.fillRect(0,0,w,h);ctx.fillStyle="#111827";ctx.textAlign="center";ctx.font="700 11px Arial";ctx.fillText((page.title||"Coloring Page").slice(0,30),w/2,19);const data=page.asset?.data;if(!data)return;const img=new Image();img.onload=()=>{const settings=page.settings||{},scale=(Number(settings.scale)||100)/100,maxW=w*.72,maxH=h*.72,ratio=Math.min(maxW/img.width,maxH/img.height)*scale,dw=img.width*ratio,dh=img.height*ratio,x=(w-dw)/2+(Number(settings.x)||0)/5,y=32+(h-42-dh)/2+(Number(settings.y)||0)/5;ctx.drawImage(img,x,y,dw,dh)};img.src=data}

function drawFallback(canvas,page){const ctx=canvas.getContext("2d");ctx.fillStyle="#fff";ctx.fillRect(0,0,canvas.width,canvas.height);ctx.fillStyle="#64748b";ctx.textAlign="center";ctx.font="700 14px Arial";ctx.fillText(page.title||"Strona",canvas.width/2,canvas.height/2)}

export function renderPageThumbnail(canvas,page){if(!canvas)return;canvas.width=180;canvas.height=233;canvas.setAttribute("aria-label",`Miniatura: ${page.title||"strona"}`);try{if(page.module==="maze-studio")drawMaze(canvas,page);else if(page.module==="coloring-studio")drawColoring(canvas,page);else drawFallback(canvas,page)}catch(error){console.error("Thumbnail render failed",error,page);drawFallback(canvas,page)}}
